[package]
name = "sinkdir"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["cargo", "string"] }

[profile.release]
# Magic incantations to produce speedier release builds
strip = "symbols"
lto = true
codegen-units = 1
panic = "abort"
